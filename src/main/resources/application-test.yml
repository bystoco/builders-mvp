spring:
  main:
    banner-mode: off
  application:
    name: Builders MVP
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:men:builders-mvp-test
    username: sa
    password:
  sql:
    init:
      mode: never
  jpa:
      hibernate:
        ddl-auto: update
      database-platform: org.hibernate.dialect.H2Dialect
